export interface Hospital{
  city_name: string,
  donations_orders?: number,
  donations_orders_cancelled?: number,
  donations_orders_done?: number,
  hospital_name: string,
  id?: string,
  state: number,
}
